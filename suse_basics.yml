---

 - hosts: 192.168.0.180 
   become: true
   tasks:
   - name: Update all packages(SuSE) to the latest version
     zypper:
            name: 
              - wireguard-tools
              - curl
              - git
              - zsh
            state: latest
